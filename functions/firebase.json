{
  "functions": {
    "predeploy": [
      "rm -rf node_modules && npm --prefix \"$RESOURCE_DIR\" cache clean --force && npm --prefix \"$RESOURCE_DIR\" install",
      "npm --prefix \"$RESOURCE_DIR\" run lint"
    ],
    "source": "functions"
  },
  "emulators": {
    "functions": {
      "port": 5001
    },
    "firestore": {
      "port": 8080
    },
    "ui": {
      "enabled": true
    }
  }
}
